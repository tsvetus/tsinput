import { TextProps } from './types';
declare const Text: ({ className, style, name, data, wait, invalid, value, onClick, onKeyDown }: TextProps) => import("react/jsx-runtime").JSX.Element;
export default Text;
