import { ListBoxProps } from './types';
declare const ListBox: ({ className, style, layout, name, data, value, wait, disabled, invalid, readOnly, placeholder, valueField, nameField, options, onClick, onKeyDown, onIconClick, onInputClick, onInputKeyDown, onChange, onSelect }: ListBoxProps) => import("react/jsx-runtime").JSX.Element;
export default ListBox;
