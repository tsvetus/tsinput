import { LabelGroupProps } from './types';
declare const LabelGroup: ({ className, style, name, data, wait, invalid, label, group, children }: LabelGroupProps) => import("react/jsx-runtime").JSX.Element;
export default LabelGroup;
