import { LabelIconProps } from './types';
/** Label icon component */
declare const LabelIcon: ({ className, style, name, data, wait, invalid, label, icon }: LabelIconProps) => import("react/jsx-runtime").JSX.Element;
export default LabelIcon;
