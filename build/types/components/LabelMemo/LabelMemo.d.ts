import { LabelMemoProps } from './types';
declare const LabelMemo: ({ className, style, layout, name, data, label, icon, wait, invalid, readOnly, placeholder, value, onLabelClick, onIconClick, onChange, onMemoClick, onMemoKeyDown }: LabelMemoProps) => import("react/jsx-runtime").JSX.Element;
export default LabelMemo;
