import { MenuItemProps } from './types';
declare const MenuItem: ({ className, style, name, data, value, children, onClick, onKeyDown }: MenuItemProps) => import("react/jsx-runtime").JSX.Element;
export default MenuItem;
